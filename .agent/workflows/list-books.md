---
description: Scan all registered project paths and display a dashboard summarizing the active phase, word counts, and critic scores for all novels.
---
1.  **Action:** Run the manifest list tool to render the directory dashboard.
    -   **Command:** `python .agent/scripts/manage_manifest.py list`
2.  **Notify:** "To switch between books, open the desired project folder in your IDE. Once inside, run `/restore-session` to re-align the agent and load that book's unique context!"
